# Jocke Ohlssons Måleri — website (draft)

Single-page landing site for **Jocke Ohlssons Måleri** (Töreboda / Skaraborg) built with **Next.js (App Router)** + **Tailwind CSS v4**.

## Development

Install dependencies:

```bash
npm install
```

Run the dev server:

```bash
npm run dev
```

Open http://localhost:3000

## Where to edit content

Most copy and business info is centralized here:

- `src/lib/content.ts`

## Images

Optimized assets live in `public/`:

- `public/logo/` — logotype
- `public/om-oss/` — team photo
- `public/projekt/` — work photos used in Tjänster, Hero and Projekt

Unoptimized originals stay in `source-photos/` (gitignored).

## Contact form (email)

The contact form posts to `/api/contact`.

To enable email sending, copy `.env.example` → `.env.local` and fill in SMTP credentials:

- `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`
- `CONTACT_TO_EMAIL`, `CONTACT_FROM_EMAIL`

If not configured, the UI tells the visitor to call instead (phone CTA).

## SEO / sharing

The site includes `robots.txt`, `sitemap.xml`, JSON-LD and an autogenerated Open Graph image.

Production URL is set in `src/lib/siteUrl.ts`.